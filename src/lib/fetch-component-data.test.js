// import sinon from 'sinon';
// import initializeConnectedComponents from './fetch-component-data';

// test('it should handle serverConnect', async() => {
//   const dispatch = fn => Promise.resolve(fn);
//   const fetchMock = jest.fn();
//   const component = {
//     WrappedComponent: {
//       serverConnect: [
//         fetchMock,
//       ],
//     },
//   };
//   await initializeConnectedComponents({
//     dispatch,
//     component,
//   });
//   expect(fetchMock).toHaveBeenCalledTimes(1);
// });
